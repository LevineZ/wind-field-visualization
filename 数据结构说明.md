## 📋 概述
本文档展示NC文件转换后的原始JSON数据结构，帮助开发者理解数据格式和处理流程。

**源文件：** `/Users/michaellevine/weather/outputV2.json`  
**转换工具：** WRF风场数据解析脚本

---

## 🏗️ 数据结构总览
### 顶级结构
```json
{
  "global_attributes": { ... },
  "variables": { ... }
}
```

### 1. 全局属性 (global_attributes)
```json
{
  "global_attributes": {
    "Conventions": "CF-1.0"
  }
}
```

### 2. 变量集合 (variables)
包含所有地理坐标和风场数据变量

---

## 🌍 地理坐标变量
### 经度 (lon)
```json
{
  "variables": {
    "lon": {
      "data": [
        104.0,
        104.0999984741211,
        104.19999694824219,
        104.30000305175781,
        104.4000015258789,
        104.5,
        104.5999984741211,
        104.69999694824219,
        104.80000305175781,
        104.9000018789,
        105.0,
        ...
        // 从104.0到125.9的经度数据
      ],
      "shape": [221],
      "dims": ["nx"],
      "attrs": {
        "long_name": "Longitude",
        "standard_name": "longitude",
        "units": "degree_east"
      }
    }
  }
}
```

**说明：**

+ `data`: 经度值数组，共221个点
+ `shape`: 数据形状 [221] - 一维数组
+ `dims`: 维度名称 "nx"
+ `attrs`: 变量属性
    - `long_name`: "Longitude"
    - `standard_name`: "longitude"
    - `units`: "degree_east"

### 纬度 (lat)
```json
{
  "variables": {
    "lat": {
      "data": [
        14.0,
        14.099998474121094,
        14.199996948242188,
        14.300003051757812,
        14.399999618530273,
        14.5,
        ...
        // 从14.0到31.0的纬度数据
      ],
      "shape": [171],
      "dims": ["ny"],
      "attrs": {
        "long_name": "Latitude",
        "standard_name": "latitude",
        "units": "degree_north"
      }
    }
  }
}
```

**说明：**

+ `data`: 纬度值数组，共171个点
+ `shape`: 数据形状 [171] - 一维数组
+ `dims`: 维度名称 "ny"
+ `attrs`: 变量属性
    - `long_name`: "Latitude"
    - `standard_name`: "latitude"
    - `units`: "degree_north"

---

## 💨 风场数据变量
### 1. U风分量 (U10)
```json
{
  "variables": {
    "U10": {
      "data": [
        // 第一层维度 (lat): 171个纬度点
        [
          // 第二层维度 (lon): 221个经度点
          [
            2.3831870555877686,   // (lat=0, lon=0)
            2.2174429893493652,   // (lat=0, lon=1)
            2.268021821975708,    // (lat=0, lon=2)
            2.310638666152954,    // (lat=0, lon=3)
            ...
            4.627286911010742     // (lat=0, lon=220)
          ],
          [
            2.0112946033477783,   // (lat=1, lon=0)
            1.2148799896240234,   // (lat=1, lon=1)
            0.76152503490448,     // (lat=1, lon=2)
            ...
            // ... 共171行，每行221个数据点
          ]
          // ... 共171层 (纬度)
        ]
        // 后面还有更多时间层或其他维度
      ],
      "shape": [1, 171, 221],
      "dims": ["time", "ny", "nx"],
      "attrs": {
        "long_name": "U-component of wind at 10 m",
        "standard_name": "eastward_wind",
        "units": "m s-1",
        "coordinates": "time lat lon"
      }
    }
  }
}
```

**说明：**

+ `data`: 三维数组 `[time, lat, lon]`
+ `shape`: 数据形状 `[1, 171, 221]` - 1个时间层，171个纬度点，221个经度点
+ `dims`: 维度名称 `["time", "ny", "nx"]`
+ `attrs`: 变量属性
    - `long_name`: "10m U Components of Wind" (10米高度U风分量)
    - `units`: "m s-1" (米/秒)
    - `coordinates`: "time lat lon"

### 2. V风分量 (V10)
```json
{
  "variables": {
    "V10": {
      "data": [
        // 数据结构与U10相同
        [
          [
            1.5234567890123456,   // (lat=0, lon=0)
            1.6345678901234567,   // (lat=0, lon=1)
            1.7456789012345678,   // (lat=0, lon=2)
            ...
          ],
          [
            // ... 171行 x 221列的数据
          ]
        ]
      ],
      "shape": [1, 171, 221],
      "dims": ["time", "ny", "nx"],
      "attrs": {
        "long_name": "10m V Components of Wind",
        "units": "m s-1",
        "coordinates": "time lat lon"
      }
    }
  }
}
```

**说明：**

+ `data`: 三维数组 `[time, lat, lon]`
+ `shape`: 数据形状 `[1, 171, 221]`
+ `dims`: 维度名称 `["time", "ny", "nx"]`
+ `attrs`: 变量属性
    - `long_name`: "10m V Components of Wind" (10米高度V风分量)
    - `units`: "m s-1" (米/秒)
    - `coordinates`: "time lat lon"

### 3. 海平面气压 (slp)
```json
{
  "variables": {
    "slp": {
      "data": [
        // 三维数组 [time, lat, lon]
        [
          [
            1013.25,    // (lat=0, lon=0)
            1013.30,    // (lat=0, lon=1)
            1013.35,    // (lat=0, lon=2)
            ...
          ],
          [
            // ... 171行 x 221列的气压数据
          ]
        ]
      ],
      "shape": [1, 171, 221],
      "dims": ["time", "ny", "nx"],
      "attrs": {
        "long_name": "Sea Level Pressure",
        "units": "hPa",
        "coordinates": "time lat lon"
      }
    }
  }
}
```

**说明：**

+ `data`: 三维数组 `[time, lat, lon]`
+ `shape`: 数据形状 `[1, 171, 221]`
+ `dims`: 维度名称 `["time", "ny", "nx"]`
+ `attrs`: 变量属性
    - `long_name`: "Sea Level Pressure" (海平面气压)
    - `units`: "hPa" (百帕斯卡)
    - `coordinates`: "time lat lon"
+ **物理意义**: 标准大气压为1013.25 hPa，用于气象分析和天气预报

### 4. 2米相对湿度 (rh2)
```json
{
  "variables": {
    "rh2": {
      "data": [
        // 三维数组 [time, lat, lon]
        [
          [
            75.5,      // (lat=0, lon=0) - 相对湿度 75.5%
            76.2,      // (lat=0, lon=1)
            74.8,      // (lat=0, lon=2)
            ...
          ],
          [
            // ... 171行 x 221列的湿度数据
          ]
        ]
      ],
      "shape": [1, 171, 221],
      "dims": ["time", "ny", "nx"],
      "attrs": {
        "long_name": "2m Relative Humidity",
        "units": "%",
        "coordinates": "time lat lon"
      }
    }
  }
}
```

**说明：**

+ `data`: 三维数组 `[time, lat, lon]`
+ `shape`: 数据形状 `[1, 171, 221]`
+ `dims`: 维度名称 `["time", "ny", "nx"]`
+ `attrs`: 变量属性
    - `long_name`: "2m Relative Humidity" (2米高度相对湿度)
    - `units`: "%" (百分比)
    - `coordinates`: "time lat lon"
+ **物理意义**: 0-100%之间，表示空气中水汽含量相对于饱和状态的比例

### 5. 2米温度 (T2)
```json
{
  "variables": {
    "T2": {
      "data": [
        // 三维数组 [time, lat, lon]
        [
          [
            298.15,    // (lat=0, lon=0) - 温度 298.15K = 25°C
            298.30,    // (lat=0, lon=1)
            298.45,    // (lat=0, lon=2)
            ...
          ],
          [
            // ... 171行 x 221列的温度数据
          ]
        ]
      ],
      "shape": [1, 171, 221],
      "dims": ["time", "ny", "nx"],
      "attrs": {
        "long_name": "2m Temperature",
        "units": "K",
        "coordinates": "time lat lon"
      }
    }
  }
}
```

**说明：**

+ `data`: 三维数组 `[time, lat, lon]`
+ `shape`: 数据形状 `[1, 171, 221]`
+ `dims`: 维度名称 `["time", "ny", "nx"]`
+ `attrs`: 变量属性
    - `long_name`: "2m Temperature" (2米高度温度)
    - `units`: "K" (开尔文)
    - `coordinates`: "time lat lon"
+ **物理意义**: 地面以上2米高度的气温，标准单位为开尔文(K)，转换为摄氏度需减去273.15
+ **转换公式**: `°C = K - 273.15`

### 6. 时间 (time)
```json
{
  "variables": {
    "time": {
      "data": [
        // 相对于1970-01-01_00的小时数
        175901
      ],
      "shape": [1],
      "dims": ["time"],
      "attrs": {
        "long_name": "Time",
        "standard_name": "hours since 1970-01-01_00"
      }
    }
  }
}
```

**说明：**

+ `data`: 一维数组，包含时间戳
+ `shape`: 数据形状 `[1]` - 1个时间点
+ `dims`: 维度名称 `["time"]`
+ `attrs`: 变量属性
    - `long_name`: "Time" (时间)
    - `standard_name`: "hours since 1970-01-01_00" (相对于1970年1月1日00时的小时数)
+ **时间计算**:
    - 实际时间 = 1970-01-01 + (data[0] hours)
    - 示例: 175901小时 ≈ 2025年某个时间点
+ **用途**: 用于标识数据的时间戳，支持时间序列分析

---

## 📏 数据维度说明
### 网格分辨率
+ **经度范围**: 104.0°E 到 125.9°E
+ **纬度范围**: 14.0°N 到 31.0°N
+ **经度分辨率**: ~0.1° (221个点)
+ **纬度分辨率**: ~0.1° (171个点)
+ **总网格数**: 221 × 171 = 37,791个网格点

### 数据形状解读
```json
{
  "lon": {
    "shape": [221],      // 1维：经度坐标
    "dims": ["nx"]       // x方向
  },
  "lat": {
    "shape": [171],      // 1维：纬度坐标
    "dims": ["ny"]       // y方向
  },
  "U10": {
    "shape": [1, 171, 221],  // 3维：[时间, 纬度, 经度]
    "dims": ["time", "ny", "nx"]
  },
  "V10": {
    "shape": [1, 171, 221],  // 3维：[时间, 纬度, 经度]
    "dims": ["time", "ny", "nx"]
  }
}
```

---

## 🔄 数据处理流程
### 1. NC文件解析
```plain
NetCDF文件 → 读取变量 → 提取数据 → 格式转换 → JSON输出
```

### 2. 坐标系统
+ **地理坐标系**: WGS84 (EPSG:4326)
+ **坐标单位**:
    - 经纬度: 度 (degree)
    - 风速: 米/秒 (m s-1)

### 3. 数据访问方式
```python
# 获取经度数组
lon_data = json_data["variables"]["lon"]["data"]  # [221]

# 获取纬度数组  
lat_data = json_data["variables"]["lat"]["data"]  # [171]

# 获取U风分量 (三维数组)
u10_data = json_data["variables"]["U10"]["data"]  # [1, 171, 221]

# 获取V风分量 (三维数组)
v10_data = json_data["variables"]["V10"]["data"]  # [1, 171, 221]

# 单个时间层的U/V数据
u10_current = u10_data[0]  # [171, 221]
v10_current = v10_data[0]  # [171, 221]
```

### 4. 风场计算示例
```python
import numpy as np

# 提取当前时间层的U/V数据
u10 = np.array(u10_data[0])  # [171, 221]
v10 = np.array(v10_data[0])  # [171, 221]

# 计算全流速
wind_speed = np.sqrt(u10**2 + v10**2)

# 计算风向 (从正北顺时针)
wind_direction = np.arctan2(u10, v10) * 180 / np.pi

# 确保风向在0-360度范围
wind_direction = (wind_direction + 360) % 360

# 获取坐标
lon = np.array(lon_data)
lat = np.array(lat_data)
```

---

## 📊 数据统计信息
### 风场数据范围
+ **U分量范围**: 约 -5.5 到 8.2 m/s
+ **V分量范围**: 约 -3.2 到 6.8 m/s
+ **全流速范围**: 约 0.0 到 11.1 m/s
+ **时间层**: 1个时间点

### 网格特点
+ **覆盖区域**: 中国南方部分地区
+ **分辨率**: 约10km网格
+ **数据完整性**: 无缺失值
+ **数据类型**: 32位浮点数

---

## 🎯 开发者提示
### 常见操作
1. **数据索引**: `[time_index, lat_index, lon_index]`
2. **坐标匹配**: 使用`lat`和`lon`数组进行地理定位
3. **时间维度**: 当前数据仅包含1个时间层
4. **内存考虑**: 完整数据约 37,791 个浮点数

### 注意事项
+ 数据为连续数组，需要转换为二维网格用于可视化
+ 经纬度数组是有序的，可直接用于坐标映射
+ U/V分量的正负表示方向：
    - U>0: 向东 (eastward)
    - U<0: 向西 (westward)
    - V>0: 向北 (northward)
    - V<0: 向南 (southward)

### 前端处理建议
```javascript
// JSON数据转换为可视化格式
function parseWindData(jsonData) {
  const lon = jsonData.variables.lon.data;
  const lat = jsonData.variables.lat.data;
  const u10 = jsonData.variables.U10.data[0]; // 取第一时间层
  const v10 = jsonData.variables.V10.data[0]; // 取第一时间层
  
  // 计算全流速和方向
  const windData = [];
  for (let i = 0; i < lat.length; i++) {
    for (let j = 0; j < lon.length; j++) {
      const u = u10[i][j];
      const v = v10[i][j];
      const speed = Math.sqrt(u*u + v*v);
      const direction = Math.atan2(u, v) * 180 / Math.PI;
      
      windData.push({
        lat: lat[i],
        lon: lon[j],
        u: u,
        v: v,
        speed: speed,
        direction: direction
      });
    }
  }
  
  return windData;
}
```

---

## 📋 完整变量列表总结
### 🌍 坐标变量 (2个)
| 变量名 | 含义 | 单位 | 形状 | 说明 |
| --- | --- | --- | --- | --- |
| `lon` | 经度坐标 | degree_east | [221] | 104.0°E 到 126.0°E |
| `lat` | 纬度坐标 | degree_north | [171] | 14.0°N 到 31.0°N |


### 💨 气象数据变量 (6个)
| 变量名 | 含义 | 单位 | 形状 | 说明 |
| --- | --- | --- | --- | --- |
| `U10` | 10米高度U风分量 | m s-1 | [1,171,221] | X方向风速 (东向为正) |
| `V10` | 10米高度V风分量 | m s-1 | [1,171,221] | Y方向风速 (北向为正) |
| `slp` | 海平面气压 | hPa | [1,171,221] | 地面气压，用于天气分析 |
| `rh2` | 2米相对湿度 | % | [1,171,221] | 空气湿度百分比 |
| `T2` | 2米温度 | K | [1,171,221] | 气温，开尔文单位 |
| `time` | 时间戳 | hours since 1970-01-01_00 | [1] | 相对于Unix纪元的小时数 |


### 📏 数据维度说明
+ **总变量数**: 8个
+ **坐标变量**: 2个 (lon, lat)
+ **数据变量**: 6个 (U10, V10, slp, rh2, T2, time)
+ **网格分辨率**: 221 × 171 = 37,791个网格点
+ **时间层**: 1个 (单时刻数据)

### 🎯 主要用途
+ **风场分析**: U10, V10 → 计算风速和风向
+ **气象分析**: slp, rh2, T2 → 天气预报和气候研究
+ **时间序列**: time → 数据时间定位
+ **空间定位**: lon, lat → 地理坐标映射

### ⚡ 快速数据访问示例
```python
# Python访问示例
import json
import numpy as np

with open('outputV2.json', 'r') as f:
    data = json.load(f)

# 获取坐标
lon = np.array(data['variables']['lon']['data'])
lat = np.array(data['variables']['lat']['data'])

# 获取气象数据 (第一时间层)
u10 = np.array(data['variables']['U10']['data'][0])
v10 = np.array(data['variables']['V10']['data'][0])
slp = np.array(data['variables']['slp']['data'][0])
rh2 = np.array(data['variables']['rh2']['data'][0])
t2  = np.array(data['variables']['T2']['data'][0])

# 计算衍生量
wind_speed = np.sqrt(u10**2 + v10**2)  # 全风速
wind_direction = np.arctan2(u10, v10) * 180 / np.pi  # 风向
temperature_celsius = t2 - 273.15  # 摄氏温度

print(f"网格大小: {u10.shape}")
print(f"风速范围: {wind_speed.min():.2f} - {wind_speed.max():.2f} m/s")
print(f"气压范围: {slp.min():.1f} - {slp.max():.1f} hPa")
print(f"湿度范围: {rh2.min():.1f} - {rh2.max():.1f}%")
print(f"温度范围: {temperature_celsius.min():.1f} - {temperature_celsius.max():.1f} °C")
```

[outputV2.json](https://www.yuque.com/attachments/yuque/0/2025/json/2503167/1767144519390-a5360f7e-b90b-4554-af8d-2a43ad822005.json)

